plugins {
    id("com.android.application")
    id("kotlin-android")
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id("dev.flutter.flutter-gradle-plugin")
}

android {
    namespace = "com.example.v_manage"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    defaultConfig {
        applicationId = "com.example.v_manage"
        minSdkVersion flutter.minSdkVersion  // Establecer aquí la versión mínima del SDK (asegúrate de que esté configurado)
        targetSdkVersion 33 // La versión del SDK de destino
        versionCode flutter.versionCode
        versionName flutter.versionName
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug
        }
    }
    kotlinOptions {
    jvmTarget = "1.8"
}
}

flutter {
    source = "../.."
}

